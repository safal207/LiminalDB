name: PR89 review fixes

on:
  push:
    branches:
      - feat/trustworthy-transition-ledger-v0.1

permissions:
  contents: write

jobs:
  apply-review-fixes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/trustworthy-transition-ledger-v0.1
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply minimal review fixes
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, found {count}")
              file.write_text(text.replace(old, new, 1))

          cargo = "liminal-db/crates/liminal-store/Cargo.toml"
          replace_once(cargo, 'crc32fast = "1"\n', 'crc32fast = "1"\nfs2 = "0.4"\n')

          wal = "liminal-db/crates/liminal-store/src/wal.rs"
          replace_once(
              wal,
              "use crc32fast::Hasher as Crc32;\nuse serde::{Deserialize, Serialize};\n",
              "use crc32fast::Hasher as Crc32;\nuse fs2::FileExt;\nuse serde::{Deserialize, Serialize};\n",
          )
          replace_once(
              wal,
              'const WAL_EXTENSION: &str = "wal";\nconst DEFAULT_SEGMENT_SIZE: u64 = 32 * 1024 * 1024; // 32 MiB\n',
              'const WAL_EXTENSION: &str = "wal";\nconst DEFAULT_SEGMENT_SIZE: u64 = 32 * 1024 * 1024; // 32 MiB\nconst WRITER_LOCK_FILE: &str = ".liminaldb-writer.lock";\n',
          )
          replace_once(
              wal,
              "    writer: WalWriter,\n    manifest_path: PathBuf,\n",
              "    writer: WalWriter,\n    manifest_path: PathBuf,\n    _writer_lock: File,\n",
          )
          replace_once(
              wal,
              '''    pub fn open<P: AsRef<Path>>(path: P) -> Result<Self> {
        let root = path.as_ref().to_path_buf();
        let data_dir = root.join("data");
        let snap_dir = root.join("snap");
        create_dir_all(&data_dir)?;
        create_dir_all(&snap_dir)?;
        let writer = WalWriter::open(&data_dir, DEFAULT_SEGMENT_SIZE)?;
        let manifest_path = root.join("manifest.cbor");
        Ok(Store {
            data_dir,
            snap_dir,
            writer,
            manifest_path,
        })
    }
''',
              '''    pub fn open<P: AsRef<Path>>(path: P) -> Result<Self> {
        let root = path.as_ref().to_path_buf();
        create_dir_all(&root)?;
        let writer_lock = acquire_writer_lock(&root)?;
        let data_dir = root.join("data");
        let snap_dir = root.join("snap");
        create_dir_all(&data_dir)?;
        create_dir_all(&snap_dir)?;
        let writer = WalWriter::open(&data_dir, DEFAULT_SEGMENT_SIZE)?;
        let manifest_path = root.join("manifest.cbor");
        Ok(Store {
            data_dir,
            snap_dir,
            writer,
            manifest_path,
            _writer_lock: writer_lock,
        })
    }
''',
          )
          replace_once(
              wal,
              "}\n\nstruct WalWriter {\n",
              '''}

fn acquire_writer_lock(root: &Path) -> Result<File> {
    let path = root.join(WRITER_LOCK_FILE);
    let mut file = OpenOptions::new()
        .create(true)
        .read(true)
        .write(true)
        .open(&path)
        .with_context(|| format!("failed to open writer lock {path:?}"))?;
    file.try_lock_exclusive()
        .with_context(|| format!("liminaldb writer lock is already held for {root:?}"))?;
    file.set_len(0)?;
    file.seek(SeekFrom::Start(0))?;
    writeln!(file, "pid={}", std::process::id())?;
    file.flush()?;
    file.sync_all()?;
    sync_directory(root)?;
    Ok(file)
}

struct WalWriter {
''',
          )
          replace_once(
              wal,
              '''        let file_path = wal_path(data_dir, segment);
        let file = OpenOptions::new()
            .create(true)
            .append(true)
            .read(true)
            .open(&file_path)
            .with_context(|| format!("failed to open wal segment {:?}", file_path))?;
        let size = file.metadata()?.len();
''',
              '''        let file_path = wal_path(data_dir, segment);
        let created = !file_path.exists();
        let file = OpenOptions::new()
            .create(true)
            .append(true)
            .read(true)
            .open(&file_path)
            .with_context(|| format!("failed to open wal segment {:?}", file_path))?;
        if created {
            file.sync_all()?;
            sync_directory(data_dir)?;
        }
        let size = file.metadata()?.len();
''',
          )
          replace_once(
              wal,
              '''    fn rotate(&mut self) -> Result<()> {
        self.segment += 1;
        self.size = 0;
        let file_path = wal_path(&self.data_dir, self.segment);
        self.file = OpenOptions::new()
            .create(true)
            .write(true)
            .read(true)
            .truncate(true)
            .open(&file_path)
            .with_context(|| format!("failed to create wal segment {:?}", file_path))?;
        Ok(())
    }
}

pub(crate) fn list_segments''',
              '''    fn rotate(&mut self) -> Result<()> {
        let next_segment = self.segment + 1;
        let file_path = wal_path(&self.data_dir, next_segment);
        let file = OpenOptions::new()
            .create(true)
            .write(true)
            .read(true)
            .truncate(true)
            .open(&file_path)
            .with_context(|| format!("failed to create wal segment {:?}", file_path))?;
        file.sync_all()?;
        sync_directory(&self.data_dir)?;
        self.segment = next_segment;
        self.size = 0;
        self.file = file;
        Ok(())
    }
}

#[cfg(unix)]
pub(crate) fn sync_directory(path: &Path) -> Result<()> {
    File::open(path)?.sync_all()?;
    Ok(())
}

#[cfg(windows)]
pub(crate) fn sync_directory(_path: &Path) -> Result<()> {
    Ok(())
}

#[cfg(not(any(unix, windows)))]
pub(crate) fn sync_directory(_path: &Path) -> Result<()> {
    Ok(())
}

pub(crate) fn list_segments''',
          )

          ledger = "liminal-db/crates/liminal-store/src/trustworthy_transition.rs"
          replace_once(
              ledger,
              "fn non_empty(value: String, label: &'static str) -> Result<String, TransitionLedgerError> {\n",
              '''fn validate_event_body(body: &TransitionEventBody) -> Result<(), TransitionLedgerError> {
    validate_canonical_identifier(&body.transition_id, "transition_id")?;
    validate_canonical_identifier(&body.subject_id, "subject_id")?;
    validate_ref(&body.record_ref, "record_ref")?;
    validate_ref(&body.payload_digest, "payload_digest")?;
    validate_optional_ref(&body.links.authorization_ref, "authorization_ref")?;
    validate_optional_ref(
        &body.links.response_integrity_ref,
        "response_integrity_ref",
    )?;
    validate_optional_ref(&body.links.causal_audit_ref, "causal_audit_ref")?;
    validate_optional_ref(
        &body.links.previous_continuity_ref,
        "previous_continuity_ref",
    )?;
    validate_optional_ref(&body.previous_event_hash, "previous_event_hash")?;
    for reference in &body.links.observation_refs {
        validate_ref(reference, "observation_ref")?;
    }
    let mut canonical_observations = body.links.observation_refs.clone();
    canonical_observations.sort();
    canonical_observations.dedup();
    if canonical_observations != body.links.observation_refs {
        return Err(TransitionLedgerError::InvalidLinks(body.kind));
    }
    if body.kind == TransitionRecordKind::ContinuitySnapshot && body.dimensions.is_none() {
        return Err(TransitionLedgerError::ContinuityDimensionsRequired);
    }
    Ok(())
}

fn validate_canonical_identifier(
    value: &str,
    label: &'static str,
) -> Result<(), TransitionLedgerError> {
    let trimmed = value.trim();
    if trimmed.is_empty() || trimmed != value {
        return Err(TransitionLedgerError::InvalidIdentifier(label));
    }
    Ok(())
}

fn non_empty(value: String, label: &'static str) -> Result<String, TransitionLedgerError> {
''',
          )
          replace_once(
              ledger,
              ''') -> Result<(), TransitionLedgerError> {
    if event.body.schema != EVENT_SCHEMA || event.body.profile != PROFILE {
''',
              ''') -> Result<(), TransitionLedgerError> {
    validate_event_body(&event.body)?;
    if event.body.schema != EVENT_SCHEMA || event.body.profile != PROFILE {
''',
          )
          replace_once(
              ledger,
              '''    if actual != expected {
        return Err(TransitionLedgerError::ParentMismatch(label));
    }
    if let Some(reference) = actual {
''',
              '''    if let Some(reference) = actual {
        if !state.record_owners.contains_key(reference) {
            return Err(TransitionLedgerError::MissingParent(reference.clone()));
        }
    }
    if actual != expected {
        return Err(TransitionLedgerError::ParentMismatch(label));
    }
    if let Some(reference) = actual {
''',
          )
          replace_once(
              ledger,
              '''    #[test]
    fn cross_transition_parent_is_rejected() {
''',
              '''    #[test]
    fn missing_parent_is_rejected() {
        let directory = tempdir().expect("tempdir");
        let mut ledger = TrustworthyTransitionLedger::open(directory.path()).expect("open");
        ledger
            .append(input(
                "transition-missing-parent",
                "agent:missing-parent",
                TransitionRecordKind::Authorization,
                "auth-missing-parent",
                TransitionLinks::default(),
            ))
            .expect("authorization");

        let missing = reference("record:missing-parent");
        let error = ledger
            .append(input(
                "transition-missing-parent",
                "agent:missing-parent",
                TransitionRecordKind::Observation,
                "obs-missing-parent",
                TransitionLinks {
                    authorization_ref: Some(missing.clone()),
                    ..TransitionLinks::default()
                },
            ))
            .expect_err("missing parent must fail");
        assert!(matches!(
            error,
            TransitionLedgerError::MissingParent(reference) if reference == missing
        ));
    }

    #[test]
    fn cross_transition_parent_is_rejected() {
''',
          )

          fixture = "liminal-db/crates/liminal-store/tests/fixtures/trustworthy_transition_ledger_v0.1.json"
          replace_once(
              fixture,
              '''    {
      "case_id": "missing_observation_from_integrity_set",
''',
              '''    {
      "case_id": "missing_parent_reference",
      "mutation": "observation references an authorization record that does not exist",
      "expected_error": "MISSING_PARENT"
    },
    {
      "case_id": "missing_observation_from_integrity_set",
''',
          )

          fixture_test = "liminal-db/crates/liminal-store/tests/trustworthy_transition_fixture.rs"
          replace_once(fixture_test, "    assert_eq!(cases.len(), 10);\n", "    assert_eq!(cases.len(), 11);\n")
          replace_once(
              fixture_test,
              '''        "PARENT_MISMATCH",
        "OBSERVATION_SET_MISMATCH",
''',
              '''        "PARENT_MISMATCH",
        "MISSING_PARENT",
        "OBSERVATION_SET_MISMATCH",
''',
          )
          replace_once(
              fixture_test,
              '''    .into_iter()
    .collect();

    let mut ids = BTreeSet::new();
''',
              '''    .into_iter()
    .collect();
    let expected_outcomes: BTreeSet<&str> = ["RECOVERED"].into_iter().collect();

    let mut ids = BTreeSet::new();
''',
          )
          replace_once(
              fixture_test,
              '''        if let Some(error) = case.get("expected_error") {
            let error = error.as_str().expect("expected_error must be a string");
            assert!(
                expected_errors.contains(error),
                "case {case_id} uses an unknown stable error code: {error}"
            );
        }
''',
              '''        if let Some(error) = case.get("expected_error") {
            let error = error.as_str().expect("expected_error must be a string");
            assert!(
                expected_errors.contains(error),
                "case {case_id} uses an unknown stable error code: {error}"
            );
        }
        if let Some(expected) = case.get("expected") {
            let expected = expected.as_str().expect("expected must be a string");
            assert!(
                expected_outcomes.contains(expected),
                "case {case_id} uses an unknown expected outcome: {expected}"
            );
        }
''',
          )
          PY

      - name: Format
        working-directory: liminal-db
        run: cargo fmt --all

      - name: Check workspace
        working-directory: liminal-db
        run: cargo check --workspace

      - name: Test liminal-store
        working-directory: liminal-db
        run: cargo test -q -p liminal-store --no-fail-fast

      - name: Commit validated fixes
        shell: bash
        run: |
          rm .github/workflows/pr89-review-fixes.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: address trustworthy ledger review findings"
          git push origin HEAD:feat/trustworthy-transition-ledger-v0.1
